# 🤖 AI Chat Guide

คู่มือการใช้งาน AI Chat ใน dLNk IDE

---

## 📋 ภาพรวม

AI Chat เป็นฟีเจอร์หลักของ dLNk IDE ที่ช่วยให้คุณ:

- เขียนโค้ดได้เร็วขึ้น
- แก้ไขข้อผิดพลาด
- เรียนรู้เทคนิคใหม่ๆ
- ได้รับคำอธิบายโค้ด
- Refactor โค้ดให้ดีขึ้น

---

## 🚀 การเปิด AI Chat

### วิธีที่ 1: Keyboard Shortcut (แนะนำ)

| ระบบ | Shortcut |
|------|----------|
| Windows/Linux | `Ctrl+Shift+A` |
| macOS | `Cmd+Shift+A` |

### วิธีที่ 2: Activity Bar

1. มองหาไอคอน **AI** ที่แถบด้านซ้าย
2. คลิกเพื่อเปิด AI Chat Panel

### วิธีที่ 3: Command Palette

1. กด `Ctrl+Shift+P` / `Cmd+Shift+P`
2. พิมพ์ "AI Chat"
3. เลือก **"dLNk: Open AI Chat"**

---

## 💬 การสนทนากับ AI

### การถามพื้นฐาน

1. พิมพ์คำถามในช่อง Chat
2. กด **Enter** เพื่อส่ง
3. รอรับคำตอบ

**ตัวอย่าง:**
```
วิธีสร้าง REST API ด้วย FastAPI
```

### การขึ้นบรรทัดใหม่

- กด `Shift+Enter` เพื่อขึ้นบรรทัดใหม่โดยไม่ส่งข้อความ

### การล้างประวัติ

- กด `Ctrl+L` / `Cmd+L` เพื่อล้างประวัติการสนทนา

---

## 📝 การใช้งานกับโค้ด

### ถาม AI เกี่ยวกับโค้ดที่เลือก

1. **เลือกโค้ด** ที่ต้องการในตัวแก้ไข
2. **เปิด AI Chat**
3. **ถามคำถาม** - AI จะเห็นโค้ดที่เลือกโดยอัตโนมัติ

**ตัวอย่าง:**
```
อธิบายโค้ดนี้ให้หน่อย
```

### คำสั่งด่วน (Quick Actions)

คลิกขวาที่โค้ดที่เลือก แล้วเลือก:

| คำสั่ง | คำอธิบาย |
|--------|----------|
| **Explain Code** | อธิบายการทำงานของโค้ด |
| **Fix Bug** | หาและแก้ไขข้อผิดพลาด |
| **Refactor** | ปรับปรุงโครงสร้างโค้ด |
| **Add Comments** | เพิ่ม Comments อธิบาย |
| **Write Tests** | สร้าง Unit Tests |
| **Optimize** | ปรับปรุงประสิทธิภาพ |

---

## 🎯 ตัวอย่างการใช้งาน

### 1. ขอให้เขียนโค้ด

```
เขียนฟังก์ชัน Python ที่รับ list ของตัวเลข แล้วคืนค่าเฉพาะเลขคู่
```

**ผลลัพธ์:**
```python
def filter_even_numbers(numbers: list[int]) -> list[int]:
    """
    กรองเฉพาะเลขคู่จาก list
    
    Args:
        numbers: list ของตัวเลข
        
    Returns:
        list ของเลขคู่
    """
    return [n for n in numbers if n % 2 == 0]
```

### 2. ขอคำอธิบายโค้ด

```
อธิบายโค้ดนี้:

async def fetch_data(url: str) -> dict:
    async with aiohttp.ClientSession() as session:
        async with session.get(url) as response:
            return await response.json()
```

**ผลลัพธ์:**
> ฟังก์ชันนี้เป็น async function ที่ใช้ดึงข้อมูลจาก URL:
> 1. สร้าง HTTP session ด้วย aiohttp
> 2. ส่ง GET request ไปยัง URL ที่ระบุ
> 3. แปลง response เป็น JSON และคืนค่า

### 3. ขอให้แก้ไขข้อผิดพลาด

```
โค้ดนี้มี bug อะไร?

def divide(a, b):
    return a / b
```

**ผลลัพธ์:**
> โค้ดนี้มีปัญหาเรื่อง Division by Zero ควรเพิ่มการตรวจสอบ:

```python
def divide(a: float, b: float) -> float:
    if b == 0:
        raise ValueError("Cannot divide by zero")
    return a / b
```

### 4. ขอให้ Refactor

```
Refactor โค้ดนี้ให้ดีขึ้น:

def process(data):
    result = []
    for item in data:
        if item['status'] == 'active':
            if item['type'] == 'user':
                result.append(item['name'])
    return result
```

**ผลลัพธ์:**
```python
def get_active_user_names(data: list[dict]) -> list[str]:
    """ดึงชื่อผู้ใช้ที่มีสถานะ active"""
    return [
        item['name'] 
        for item in data 
        if item.get('status') == 'active' and item.get('type') == 'user'
    ]
```

---

## ⌨️ Keyboard Shortcuts

| การกระทำ | Windows/Linux | macOS |
|----------|---------------|-------|
| เปิด AI Chat | `Ctrl+Shift+A` | `Cmd+Shift+A` |
| ส่งข้อความ | `Enter` | `Enter` |
| ขึ้นบรรทัดใหม่ | `Shift+Enter` | `Shift+Enter` |
| ล้างประวัติ | `Ctrl+L` | `Cmd+L` |
| คัดลอกคำตอบ | `Ctrl+C` | `Cmd+C` |
| ปิด AI Chat | `Esc` | `Esc` |

---

## 💡 เคล็ดลับการใช้งาน

### 1. ถามอย่างเฉพาะเจาะจง

❌ **ไม่ดี:**
```
แก้โค้ดให้หน่อย
```

✅ **ดี:**
```
แก้ TypeError ในฟังก์ชัน calculate_total ที่บรรทัด 25
```

### 2. ให้ Context

❌ **ไม่ดี:**
```
เขียน API
```

✅ **ดี:**
```
เขียน REST API ด้วย FastAPI สำหรับ CRUD operations ของ User model
```

### 3. ถามต่อเนื่อง

AI จำบริบทการสนทนา คุณสามารถถามต่อได้:

```
User: เขียนฟังก์ชันอ่านไฟล์ JSON
AI: [ให้โค้ด]
User: เพิ่ม error handling ด้วย
AI: [ให้โค้ดที่ปรับปรุงแล้ว]
User: เขียน unit test ให้ด้วย
AI: [ให้ test code]
```

### 4. ขอทางเลือก

```
มีวิธีอื่นในการทำ X ไหม?
```

### 5. ขอคำอธิบายเพิ่มเติม

```
อธิบายเพิ่มเติมเกี่ยวกับ Y ได้ไหม?
```

---

## ⚠️ ข้อจำกัด

| รายการ | ค่า |
|--------|-----|
| ความยาว Prompt สูงสุด | 4,000 ตัวอักษร |
| Rate Limit | 60 requests/นาที |
| ขนาดโค้ดที่ส่งได้ | 10,000 ตัวอักษร |

### หัวข้อที่ถูกจำกัด

AI อาจปฏิเสธคำถามเกี่ยวกับ:
- การโจมตีระบบ dLNk
- การ Bypass License
- การขโมยข้อมูลส่วนตัว
- กิจกรรมผิดกฎหมาย

---

## 🔧 การแก้ไขปัญหา

### AI ไม่ตอบ

1. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
2. ตรวจสอบสถานะ License ที่ Status Bar
3. ลองล้างประวัติแล้วถามใหม่

### คำตอบไม่ถูกต้อง

1. ให้ Context มากขึ้น
2. ถามใหม่อย่างเฉพาะเจาะจง
3. แจ้งว่าคำตอบไม่ถูกต้องและขอให้แก้ไข

### Rate Limit

หากถึง Rate Limit:
1. รอ 1 นาที
2. ลดความถี่ในการถาม
3. รวมหลายคำถามเป็นคำถามเดียว

---

## 📊 สถิติการใช้งาน

ดูสถิติการใช้งาน AI ได้ที่:

1. คลิกไอคอน **License** ที่ Status Bar
2. ดูจำนวน Requests ที่ใช้ไป
3. ดูโควต้าที่เหลือ

---

## ❓ คำถามที่พบบ่อย

### Q: AI เห็นไฟล์ทั้งหมดในโปรเจ็คไหม?

A: ไม่ AI เห็นเฉพาะโค้ดที่คุณเลือกหรือไฟล์ที่เปิดอยู่

### Q: ประวัติการสนทนาเก็บไว้ที่ไหน?

A: เก็บในเครื่องของคุณ ไม่ส่งไปเซิร์ฟเวอร์

### Q: ใช้ได้แบบ Offline ไหม?

A: ไม่ได้ ต้องเชื่อมต่ออินเทอร์เน็ต

---

**ก่อนหน้า:** [← เริ่มต้นใช้งาน](getting-started.md)  
**ถัดไป:** [Code Completion →](code-completion.md)

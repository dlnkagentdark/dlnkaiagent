# ⚡ Code Completion Guide

คู่มือการใช้งาน AI Code Completion ใน dLNk IDE

---

## 📋 ภาพรวม

AI Code Completion ช่วยให้คุณเขียนโค้ดได้เร็วขึ้นโดย:

- แนะนำโค้ดอัตโนมัติขณะพิมพ์
- เติมเต็มฟังก์ชัน, คลาส, และ methods
- แนะนำ imports ที่จำเป็น
- เขียน boilerplate code ให้

---

## 🚀 การใช้งาน

### การยอมรับคำแนะนำ

เมื่อ AI แนะนำโค้ด (แสดงเป็นสีเทา):

| การกระทำ | ปุ่ม |
|----------|------|
| ยอมรับทั้งหมด | `Tab` |
| ยอมรับทีละคำ | `Ctrl+→` / `Cmd+→` |
| ปิดคำแนะนำ | `Esc` |
| ดูคำแนะนำถัดไป | `Alt+]` |
| ดูคำแนะนำก่อนหน้า | `Alt+[` |

### ตัวอย่างการใช้งาน

#### 1. เติมเต็มฟังก์ชัน

พิมพ์:
```python
def calculate_area(
```

AI แนะนำ:
```python
def calculate_area(width: float, height: float) -> float:
    """Calculate the area of a rectangle."""
    return width * height
```

#### 2. เติมเต็ม Import

พิมพ์:
```python
import json

data = json.lo
```

AI แนะนำ:
```python
data = json.loads(json_string)
```

#### 3. เติมเต็ม Loop

พิมพ์:
```python
for i in 
```

AI แนะนำ:
```python
for i in range(10):
    print(i)
```

---

## ⚙️ การตั้งค่า

### เปิด/ปิด Code Completion

1. ไปที่ **Settings** (`Ctrl+,` / `Cmd+,`)
2. ค้นหา "dLNk AI Completion"
3. เปิด/ปิดตามต้องการ

### ตั้งค่าผ่าน settings.json

```json
{
  "dlnk.ai.completion.enabled": true,
  "dlnk.ai.completion.delay": 100,
  "dlnk.ai.completion.maxSuggestions": 3,
  "dlnk.ai.completion.triggerOnTyping": true
}
```

### รายละเอียดการตั้งค่า

| การตั้งค่า | ค่าเริ่มต้น | คำอธิบาย |
|------------|-------------|----------|
| `enabled` | `true` | เปิด/ปิด AI Completion |
| `delay` | `100` | หน่วงเวลาก่อนแสดงคำแนะนำ (ms) |
| `maxSuggestions` | `3` | จำนวนคำแนะนำสูงสุด |
| `triggerOnTyping` | `true` | แสดงคำแนะนำขณะพิมพ์ |

---

## 🎯 ภาษาที่รองรับ

AI Code Completion รองรับภาษาต่อไปนี้:

| ภาษา | ระดับการรองรับ |
|------|----------------|
| Python | ⭐⭐⭐⭐⭐ |
| JavaScript | ⭐⭐⭐⭐⭐ |
| TypeScript | ⭐⭐⭐⭐⭐ |
| Java | ⭐⭐⭐⭐ |
| C/C++ | ⭐⭐⭐⭐ |
| Go | ⭐⭐⭐⭐ |
| Rust | ⭐⭐⭐⭐ |
| PHP | ⭐⭐⭐ |
| Ruby | ⭐⭐⭐ |
| Swift | ⭐⭐⭐ |
| Kotlin | ⭐⭐⭐ |
| HTML/CSS | ⭐⭐⭐ |
| SQL | ⭐⭐⭐ |
| Shell/Bash | ⭐⭐⭐ |

---

## 💡 เคล็ดลับ

### 1. เขียน Comments ก่อน

AI จะแนะนำโค้ดตาม Comment:

```python
# Function to validate email address using regex
def validate_email
```

AI จะแนะนำฟังก์ชันที่ตรงกับ Comment

### 2. ใช้ชื่อที่สื่อความหมาย

```python
# ชื่อที่ดี - AI เข้าใจง่าย
def calculate_monthly_salary(hours_worked, hourly_rate):

# ชื่อที่ไม่ดี - AI เดายาก
def calc(x, y):
```

### 3. เขียน Type Hints

```python
# มี Type Hints - AI แนะนำได้แม่นยำ
def process_data(data: list[dict]) -> pd.DataFrame:

# ไม่มี Type Hints - AI เดาได้ยาก
def process_data(data):
```

### 4. ใช้ Docstrings

```python
def fetch_user(user_id: int) -> User:
    """
    Fetch user from database by ID.
    
    Args:
        user_id: The unique identifier of the user
        
    Returns:
        User object if found
        
    Raises:
        UserNotFoundError: If user doesn't exist
    """
```

---

## ⌨️ Keyboard Shortcuts

| การกระทำ | Windows/Linux | macOS |
|----------|---------------|-------|
| ยอมรับคำแนะนำ | `Tab` | `Tab` |
| ยอมรับทีละคำ | `Ctrl+→` | `Cmd+→` |
| ปิดคำแนะนำ | `Esc` | `Esc` |
| ขอคำแนะนำ | `Ctrl+Space` | `Cmd+Space` |
| คำแนะนำถัดไป | `Alt+]` | `Option+]` |
| คำแนะนำก่อนหน้า | `Alt+[` | `Option+[` |

---

## 🔧 การแก้ไขปัญหา

### คำแนะนำไม่แสดง

1. ตรวจสอบว่าเปิด AI Completion อยู่
2. ตรวจสอบการเชื่อมต่ออินเทอร์เน็ต
3. ตรวจสอบสถานะ License

### คำแนะนำไม่ถูกต้อง

1. เพิ่ม Context (Comments, Type Hints)
2. ใช้ชื่อตัวแปรที่สื่อความหมาย
3. กด `Alt+]` ดูคำแนะนำอื่น

### คำแนะนำช้า

1. เพิ่มค่า `delay` ใน Settings
2. ตรวจสอบความเร็วอินเทอร์เน็ต
3. ลดจำนวน Extensions ที่ใช้งาน

---

## 📊 เปรียบเทียบกับ IntelliSense

| คุณสมบัติ | IntelliSense | AI Completion |
|-----------|--------------|---------------|
| แนะนำจาก Syntax | ✅ | ✅ |
| แนะนำจาก Context | ❌ | ✅ |
| เขียนฟังก์ชันทั้งหมด | ❌ | ✅ |
| เข้าใจ Comments | ❌ | ✅ |
| แนะนำ Algorithm | ❌ | ✅ |
| ต้องการ Internet | ❌ | ✅ |

---

## ❓ คำถามที่พบบ่อย

### Q: AI Completion ใช้ได้กับทุกไฟล์ไหม?

A: ใช้ได้กับภาษาที่รองรับ ดูรายการด้านบน

### Q: ใช้ได้แบบ Offline ไหม?

A: ไม่ได้ ต้องเชื่อมต่ออินเทอร์เน็ต

### Q: มีค่าใช้จ่ายเพิ่มเติมไหม?

A: รวมอยู่ใน License แล้ว ไม่มีค่าใช้จ่ายเพิ่ม

### Q: ปิด AI Completion แต่ใช้ IntelliSense ได้ไหม?

A: ได้ IntelliSense ยังทำงานปกติ

---

**ก่อนหน้า:** [← AI Chat](ai-chat.md)  
**ถัดไป:** [Keyboard Shortcuts →](shortcuts.md)

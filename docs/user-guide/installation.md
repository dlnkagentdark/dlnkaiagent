# 📥 วิธีติดตั้ง dLNk IDE

คู่มือการติดตั้ง dLNk IDE สำหรับทุกระบบปฏิบัติการ

---

## 💻 ความต้องการของระบบ

### ขั้นต่ำ

| รายการ | ความต้องการ |
|--------|-------------|
| **ระบบปฏิบัติการ** | Windows 10/11, Ubuntu 20.04+, macOS 11+ |
| **RAM** | 4 GB |
| **พื้นที่ดิสก์** | 500 MB |
| **อินเทอร์เน็ต** | จำเป็นสำหรับการลงทะเบียน |

### แนะนำ

| รายการ | ความต้องการ |
|--------|-------------|
| **RAM** | 8 GB ขึ้นไป |
| **พื้นที่ดิสก์** | 1 GB |
| **หน้าจอ** | 1920x1080 ขึ้นไป |

---

## 🪟 Windows

### ขั้นตอนการติดตั้ง

1. **ดาวน์โหลด** ไฟล์ `dLNk-IDE-Setup.exe` จากหน้า releases
2. **เปิดไฟล์** ที่ดาวน์โหลดมา
3. **ทำตามขั้นตอน** ของ Installation Wizard
4. **เลือกตำแหน่ง** ที่ต้องการติดตั้ง (แนะนำ: ค่าเริ่มต้น)
5. **รอการติดตั้ง** เสร็จสิ้น
6. **เปิด dLNk IDE** จาก Start Menu

### การแก้ไขปัญหา Windows

#### "Windows protected your PC"

หากพบข้อความนี้:

1. คลิก **"More info"**
2. คลิก **"Run anyway"**

#### "Missing DLL" Error

```powershell
# ติดตั้ง Visual C++ Redistributable
winget install Microsoft.VCRedist.2015+.x64
```

---

## 🐧 Linux

### Ubuntu/Debian

#### วิธีที่ 1: AppImage (แนะนำ)

```bash
# ดาวน์โหลด AppImage
wget https://releases.dlnk.io/dLNk-IDE.AppImage

# ให้สิทธิ์ execute
chmod +x dLNk-IDE.AppImage

# รันแอพ
./dLNk-IDE.AppImage
```

#### วิธีที่ 2: .deb Package

```bash
# ดาวน์โหลด .deb
wget https://releases.dlnk.io/dLNk-IDE.deb

# ติดตั้ง
sudo dpkg -i dLNk-IDE.deb

# แก้ไข dependencies (ถ้ามี)
sudo apt-get install -f
```

### Fedora/RHEL

```bash
# ดาวน์โหลด .rpm
wget https://releases.dlnk.io/dLNk-IDE.rpm

# ติดตั้ง
sudo rpm -i dLNk-IDE.rpm
```

### Arch Linux

```bash
# ใช้ AUR helper
yay -S dlnk-ide
```

### การแก้ไขปัญหา Linux

#### AppImage ไม่ทำงาน

```bash
# ติดตั้ง FUSE
sudo apt install libfuse2

# หรือสำหรับ FUSE3
sudo apt install fuse3
```

#### ไม่มีไอคอนใน Application Menu

```bash
# สร้าง desktop entry
cat > ~/.local/share/applications/dlnk-ide.desktop << EOF
[Desktop Entry]
Name=dLNk IDE
Exec=/path/to/dLNk-IDE.AppImage
Icon=dlnk-ide
Type=Application
Categories=Development;IDE;
EOF
```

---

## 🍎 macOS

### ขั้นตอนการติดตั้ง

1. **ดาวน์โหลด** ไฟล์ `dLNk-IDE.dmg` จากหน้า releases
2. **เปิดไฟล์** .dmg
3. **ลาก** ไอคอน dLNk IDE ไปยังโฟลเดอร์ Applications
4. **เปิด** dLNk IDE จาก Applications

### การแก้ไขปัญหา macOS

#### "App is damaged and can't be opened"

```bash
# ลบ quarantine attribute
xattr -cr /Applications/dLNk-IDE.app
```

#### "App can't be opened because Apple cannot check it"

1. ไปที่ **System Preferences** → **Security & Privacy**
2. คลิก **"Open Anyway"** ที่ปรากฏ

#### สำหรับ Apple Silicon (M1/M2/M3)

```bash
# ติดตั้ง Rosetta 2 (ถ้าจำเป็น)
softwareupdate --install-rosetta
```

---

## 🔐 การเปิดใช้งานครั้งแรก

### 1. เปิด dLNk IDE

หลังติดตั้งเสร็จ ให้เปิดแอพ dLNk IDE

### 2. ลงทะเบียน (สำหรับผู้ใช้ใหม่)

1. คลิก **"Register"**
2. กรอกข้อมูล:
   - อีเมล
   - ชื่อผู้ใช้
   - รหัสผ่าน
3. คลิก **"Submit"**
4. รอรับ License Key ทางอีเมลหรือ Telegram

### 3. เข้าสู่ระบบ (สำหรับผู้ใช้ที่มี License แล้ว)

1. คลิก **"Login"**
2. ใส่ **License Key** ในรูปแบบ:
   ```
   DLNK-XXXX-XXXX-XXXX-XXXX
   ```
3. คลิก **"Activate"**

### 4. เริ่มใช้งาน

หลังจาก Activate สำเร็จ คุณสามารถเริ่มใช้งาน AI features ได้ทันที!

---

## 🔄 การอัพเดท

### อัพเดทอัตโนมัติ

dLNk IDE จะตรวจสอบการอัพเดทอัตโนมัติเมื่อเปิดแอพ

### อัพเดทด้วยตนเอง

1. ไปที่ **Help** → **Check for Updates**
2. หากมีเวอร์ชันใหม่ คลิก **"Download Update"**
3. รีสตาร์ทแอพเพื่อติดตั้ง

---

## 🗑️ การถอนการติดตั้ง

### Windows

1. ไปที่ **Settings** → **Apps** → **Apps & features**
2. ค้นหา "dLNk IDE"
3. คลิก **"Uninstall"**

### Linux

```bash
# สำหรับ .deb
sudo apt remove dlnk-ide

# สำหรับ .rpm
sudo rpm -e dlnk-ide

# สำหรับ AppImage
rm ~/dLNk-IDE.AppImage
```

### macOS

1. เปิด **Finder** → **Applications**
2. ลาก dLNk IDE ไปยัง **Trash**
3. ล้าง Trash

---

## ❓ ต้องการความช่วยเหลือ?

หากพบปัญหาในการติดตั้ง:

- ดู [FAQ](faq.md)
- ติดต่อ Telegram: @dlnk_support
- อีเมล: support@dlnk.io

---

**ก่อนหน้า:** [← User Guide](README.md)  
**ถัดไป:** [เริ่มต้นใช้งาน →](getting-started.md)
